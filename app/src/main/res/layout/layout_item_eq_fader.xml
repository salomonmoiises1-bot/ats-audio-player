<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="52dp"
    android:layout_height="230dp"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:paddingStart="2dp"
    android:paddingTop="4dp"
    android:paddingEnd="2dp"
    android:paddingBottom="4dp">

    <!-- Frequency Label (top) ej: 20Hz, 1kHz -->
    <TextView
        android:id="@+id/freqLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:ellipsize="none"
        android:gravity="center"
        android:singleLine="true"
        android:text="20Hz"
        android:textColor="#00E5FF"
        android:textSize="10sp"
        android:textStyle="bold" />

    <!-- Gain Label (live) ej: +3.2dB / 0.0dB -->
    <TextView
        android:id="@+id/gainLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:layout_marginBottom="2dp"
        android:gravity="center"
        android:singleLine="true"
        android:text="0.0dB"
        android:textColor="#E0E0E0"
        android:textSize="10sp" />

    <!-- Custom VerticalSeekBar rotado 270° / -90° (Range: -12dB a +12dB, centro en 0dB) -->
    <com.ats.audioplayer.VerticalSeekBar
        android:id="@+id/skBand"
        android:layout_width="32dp"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:max="240"
        android:progress="120"
        android:progressTint="#00E5FF"
        android:splitTrack="false"
        android:thumbTint="#FFFFFF" />

    <!-- Tiny reset button -->
    <Button
        android:id="@+id/btnReset"
        style="@style/Widget.Material3.Button.TextButton"
        android:layout_width="48dp"
        android:layout_height="28dp"
        android:layout_marginTop="2dp"
        android:insetTop="0dp"
        android:insetBottom="0dp"
        android:minWidth="0dp"
        android:minHeight="0dp"
        android:padding="0dp"
        android:text="0"
        android:textColor="#808080"
        android:textSize="9sp" />

</LinearLayout>